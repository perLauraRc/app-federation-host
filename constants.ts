export const HOSTNAME = 'app-federation'
export const BASE_URL = `http://${HOSTNAME}`
export const REMOTE_URL = `http://${HOSTNAME}-remote`
export const HOST_PORT = 5173
export const REMOTE_PORT = 5174
